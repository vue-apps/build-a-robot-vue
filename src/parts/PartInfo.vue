<template>
  <div>
    <h1>{{ part.title }}</h1>
    <div>
      {{ part.description }}
    </div>
  </div>
</template>

<script>
import parts from '@/data/parts';
export default {
  name: 'PartInfo',
  computed: {
    part() {
      const { partType } = this.$route.params;
      const { id } = this.$route.params;
      console.log(parts[partType].find((part) => part.id === +id));
      return parts[partType].find((part) => part.id === +id);
    },
  },
};
</script>
